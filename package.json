{
  "name": "pft-analytics",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "monitor:reset": "node scripts/reset_monitor.mjs",
    "monitor:reset:once": "node scripts/reset_monitor.mjs --once"
  },
  "dependencies": {
    "@vercel/blob": "^2.0.1",
    "@vercel/node": "^5.5.28",
    "xrpl": "^4.5.0"
  },
  "devDependencies": {
    "@types/node": "^25.2.0",
    "typescript": "~5.9.3",
    "vite": "^7.2.4"
  }
}
